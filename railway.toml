[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push && npx next start -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
