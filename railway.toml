[build]
builder = "NIXPACKS"

[deploy]
startCommand = "bash -c 'npx prisma db push && npx next start -p ${PORT:-3000}'"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
